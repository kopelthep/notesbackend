{
  "name": "backend",
  "version": "0.0.1",
  "description": "",
  "license": "ARR",
  "author": "Paul BOULESTEIX",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../FullStackOpen/part2/notes && npm run build && cp -r dist ../../../notesbackend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  }
}
